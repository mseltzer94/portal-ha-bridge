# ── Signing secrets — NEVER share. Keep locally: losing them means you can
#    never sign an update that installs over the existing app. ──
keystore.properties
*.keystore
*.jks

# Build outputs
/build
/app/build
.gradle/
*.apk

# Auto-downloaded by provision.ps1 when adb isn't on PATH
/platform-tools/

# Android Studio / IDE (machine-specific; regenerated on open)
.idea/
*.iml
local.properties
.DS_Store
portal_ha_backup.xml
